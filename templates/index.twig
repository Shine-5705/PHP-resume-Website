<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ seo.title }}</title>
    
    <meta name="description" content="{{ seo.description }}">
    <meta name="keywords" content="{{ seo.keywords }}">
    <meta name="author" content="{{ seo.author }}">
    <meta name="robots" content="index, follow">
    <link rel="canonical" href="{{ seo.url }}">
    
    <meta property="og:title" content="{{ seo.title }}">
    <meta property="og:description" content="{{ seo.description }}">
    <meta property="og:image" content="{{ seo.image }}">
    <meta property="og:url" content="{{ seo.url }}">
    <meta property="og:type" content="{{ seo.type }}">
    <meta property="og:locale" content="{{ seo.locale }}">
    <meta property="og:site_name" content="{{ seo.site_name }}">
    
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ seo.title }}">
    <meta name="twitter:description" content="{{ seo.description }}">
    <meta name="twitter:image" content="{{ seo.image }}">
    <meta name="twitter:creator" content="@shine_gupta17">
    <meta name="twitter:site" content="@shine_gupta17">
    
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:image:alt" content="Shine Gupta - AI Engineer & Full-Stack Developer">
    
    <meta name="theme-color" content="#ffffff">
    <meta name="application-name" content="{{ seo.site_name }}">
    <meta name="msapplication-TileColor" content="#ffffff">
    
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    
    <link rel="preload" href="/css/main.min.css" as="style">
    <link rel="preload" href="/js/main.min.js" as="script">
    <link rel="stylesheet" href="/css/main.min.css">
    
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "Person",
        "name": "Shine Gupta",
        "jobTitle": "AI Engineer & Full-Stack Developer",
        "description": "{{ seo.description }}",
        "url": "{{ seo.url }}",
        "image": "{{ seo.image }}",
        "address": {
            "@type": "PostalAddress",
            "addressLocality": "Bengaluru",
            "addressCountry": "India"
        },
        "sameAs": [
            "https://github.com/Shine-5705",
            "https://www.linkedin.com/in/shine-gupta-62b22b264",
            "https://scholar.google.com/citations?user=kx3_tLYAAAAJ&hl=en",
            "https://x.com/shine_gupta17"
        ]
    }
    </script>
</head>
<body>
    <main class="container">
        <div class="content">
            <div class="header-section">
                <div class="profile-info">
                    <h1>{{ profile.name }}</h1>
                    <p class="description">{{ profile.description }}</p>
                    <div class="location">
                        <a class="profile-location" href="https://www.google.com/maps/place/Bengaluru" target="_blank">
                            <div class="contact-link">
                                <img src="assets/icons/browser.png" alt="Link to the person location" />
                            </div>
                            <p>{{ profile.location }}</p> 
                        </a>
                    </div>
                    
                    <div class="contact-links">
                        <a href="https://shinegupta.netlify.app/" class="contact-link" target="_blank"><img src="assets/icons/browser.png" alt="Link to the website" /></a>
                        <a href="mailto:guptashine5002@gmail.com" class="contact-link" target="_blank"><img src="assets/icons/mail.png" alt="Link to the email" /></a>
                        <a href="https://github.com/Shine-5705" class="contact-link" target="_blank"><img src="assets/icons/github.png" alt="Link to the github repo" /></a>
                        
                        <a href="https://scholar.google.com/citations?user=kx3_tLYAAAAJ&hl=en" class="contact-link" target="_blank" rel="noopener noreferrer"><img src="assets/icons/scholar.svg" alt="Link to Google Scholar profile" /></a>
                        <a href="https://www.linkedin.com/in/shine-gupta-62b22b26" class="contact-link" target="_blank"><img src="assets/icons/linkedin.png" alt="Link to the linkedin profile" /></a>
                        <a href="https://x.com/shine_gupta17" class="contact-link" target="_blank"><img src="assets/icons/x.svg" alt="Link to the twitter/X profile" /></a>
                    </div>
                </div>
                
                <div class="profile-image">
                    <img src="assets/images/profile.webp" alt="Image of {{ profile.name }}" />
                </div>
            </div>
            
            <div id="about-section">
                <div class="about-info">
                    <h2>About</h2>
                    <div class="about-description">{{ profile.about }}</div>
                </div>
            </div>
            
            <section class="work-experience">
                <h2 id="work-experience">Work Experience</h2>
                <div class="work-list">
                    {% for job in workExperience %}
                    <article class="work-item">
                        <div class="work-content">
                            <div class="work-header">
                                <div class="work-title-section">
                                    <h3 class="work-company">
                                        <a href="{{ job.url }}" target="_blank" rel="noopener noreferrer">{{ job.company }}</a>
                                        <ul class="work-tags work-tags-desktop">
                                            {% for tag in job.tags %}
                                            <li><div class="tag">{{ tag }}</div></li>
                                            {% endfor %}
                                        </ul>
                                    </h3>
                                    <div class="work-period">{{ job.period }}</div>
                                </div>
                                <h4 class="work-position">{{ job.position }}{% if job.location %} · {{ job.location }}{% endif %}</h4>
                            </div>
                            <div class="work-description">
                                <div class="work-summary">{{ job.description }}</div>
                                {% if job.details %}
                                <ul class="work-details">
                                    {% for detail in job.details %}
                                    <li>{{ detail }}</li>
                                    {% endfor %}
                                </ul>
                                {% endif %}
                                <div class="work-tags-mobile">
                                    <ul class="work-tags">
                                        {% for tag in job.tags %}
                                        <li><div class="tag">{{ tag }}</div></li>
                                        {% endfor %}
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </article>
                    {% endfor %}
                </div>
            </section>
            
            <section class="education-section">
                <h2 id="education-section">Education</h2>
                <div class="education-list">
                    {% for edu in education %}
                    <article class="education-item">
                        <div class="education-content">
                            <div class="education-header">
                                <h3 class="education-institution">{{ edu.institution }}</h3>
                                <div class="education-period">{{ edu.period }}</div>
                            </div>
                            <div class="education-degree">{{ edu.degree }}</div>
                        </div>
                    </article>
                    {% endfor %}
                </div>
            </section>

            <section class="publications-section">
                <h2 id="publications">Publications</h2>
                <div class="publications-list">
                    {% for pub in publications %}
                    <article class="publication-item">
                        <div class="publication-content">
                            <div class="publication-header">
                                <h3 class="publication-title">{{ pub.title }}</h3>
                                <div class="publication-meta">
                                    <span class="publication-status">{{ pub.status }}</span>
                                    <span class="publication-period">{{ pub.period }}</span>
                                </div>
                            </div>
                            <div class="publication-authors">{{ pub.authors }}</div>
                            <div class="publication-venue">{{ pub.venue }}</div>
                        </div>
                    </article>
                    {% endfor %}
                </div>
            </section>

            <section class="hackathons-section">
                <h2 id="hackathons">Hackathons</h2>
                <p class="section-intro">9+ hackathon wins — a glimpse of recent highlights.</p>
                <div class="hackathons-list">
                    {% for hackathon in hackathons %}
                    <article class="hackathon-item">
                        <div class="hackathon-content">
                            <div class="hackathon-header">
                                <div class="hackathon-title-section">
                                    <h3 class="hackathon-title">
                                        {{ hackathon.title }}
                                        <span class="hackathon-result">{{ hackathon.result }}</span>
                                    </h3>
                                    <div class="hackathon-period">{{ hackathon.period }} · {{ hackathon.location }}</div>
                                </div>
                            </div>
                            <div class="hackathon-project">Project: <strong>{{ hackathon.project }}</strong></div>
                            <div class="hackathon-description">{{ hackathon.description }}</div>
                            {% if hackathon.highlights %}
                            <ul class="hackathon-highlights">
                                {% for highlight in hackathon.highlights %}
                                <li>{{ highlight }}</li>
                                {% endfor %}
                            </ul>
                            {% endif %}
                            <div class="hackathon-footer">
                                <ul class="hackathon-tags">
                                    {% for tag in hackathon.tags %}
                                    <li><div class="tag">{{ tag }}</div></li>
                                    {% endfor %}
                                </ul>
                                <a href="{{ hackathon.linkedin }}" class="hackathon-link" target="_blank" rel="noopener noreferrer">View LinkedIn post →</a>
                            </div>
                        </div>
                    </article>
                    {% endfor %}
                </div>
            </section>

            <section class="skills-section">
                <h2 id="skills-section">Skills</h2>
                <div class="skills-categories">
                    {% for category, skills in skillCategories %}
                    <div class="skill-category">
                        <h3 class="skill-category-title">{{ category }}</h3>
                        <ul class="skills-list">
                            {% for skill in skills %}
                            <li><div class="skill-tag">{{ skill }}</div></li>
                            {% endfor %}
                        </ul>
                    </div>
                    {% endfor %}
                </div>
            </section>

            <section class="projects-section">
                <h2 id="side-projects">Projects</h2>
                <div class="projects-grid">
                    {% for project in sideProjects %}
                    <article class="project-card">
                        <div class="project-content">
                            <div class="project-header">
                                <div class="project-title-section">
                                    <h3 class="project-title">
                                        <a href="{{ project.url }}" target="_blank" rel="noopener noreferrer">
                                            {{ project.name }}
                                            {% if project.active %}
                                            <span class="active-indicator"></span>
                                            {% endif %}
                                        </a>
                                        <div class="project-url">{{ project.url|replace({'https://': '', 'http://': ''}) }}</div>
                                    </h3>
                                    <p class="project-description">{{ project.description }}</p>
                                </div>
                            </div>
                            <div class="project-footer">
                                <ul class="project-tags">
                                    {% for tech in project.technologies %}
                                    <li><div class="project-tag">{{ tech }}</div></li>
                                    {% endfor %}
                                </ul>
                            </div>
                        </div>
                    </article>
                    {% endfor %}
                </div>
            </section>
        </div>
    </main>

    <script src="/js/main.min.js"></script>
</body>
</html>
